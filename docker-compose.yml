services:
  ipam:
    build: .
    network_mode: host
    cap_add:
      - NET_ADMIN
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
      - HTTPS_PORT=8443
      - HTTP_PORT=8080
    restart: unless-stopped
